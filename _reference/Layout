
Runner: SINGLETON
Runner starts
    Creates handler
    Starts handler
    Finds files

    for files (threaded)
        create test object
        load file
        run file tests
    end
Runner ends

Handler:
handler starts
    Creates listener

    result or diag recieved
        switch to subprocess handler is in subprocess
        send result to each handler
    end
handler stops


